<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:websocket="http://www.springframework.org/schema/websocket"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/websocket http://www.springframework.org/schema/websocket/spring-websocket.xsd">

	<websocket:message-broker
		application-destination-prefix="/app">
		<websocket:stomp-endpoint path="/marcopolo">
			<websocket:sockjs websocket-enabled="true"></websocket:sockjs>
		</websocket:stomp-endpoint>
		<!-- 代理目的地，而已/app开头的都会路由到带有@MessageMapping的注解方法中 -->
		<websocket:simple-broker prefix="/queue,/topic" />
		<!-- <websocket:stomp-broker-relay prefix="/queue,/topic" relay-host="${rabbitmq.host}" 
			relay-port="${rabbitmq.port}" client-login="guest" client-passcode="guest" 
			auto-startup="true" /> -->
	</websocket:message-broker>

</beans>
